
import '../constants/app_strings.dart';

class Content {
  final String headText, text, image;
  Content(
      {required this.headText,
        required this.text,
        required this.image}
      );
}

